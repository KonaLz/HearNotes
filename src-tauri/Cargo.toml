[package]
name = "meeting-scribe"
version = "1.0.1"
description = "Meeting Scribe local transcription and speaker separation"
authors = ["KonaLz"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"
tauri-plugin-updater = "2"

[features]
custom-protocol = ["tauri/custom-protocol"]
